- if @post.errors.present?
  = notice_stream(message: :error, status: 'red')
  = form_post_stream(post: @post)
- else
  = notice_stream(message: :create, status: 'green')
  = turbo_stream.replace :form_post do
    = turbo_frame_tag :form_post
  = turbo_stream.prepend :postlists, partial: 'post', locals: { post: @post }
